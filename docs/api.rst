.. _api:

Python API
==========

*imo-vmdb* exposes two Python APIs that can be used independently of any web
framework. The **API** (``imo_vmdb``) covers importing, normalising, cleaning up,
and exporting data.

.. automodule:: imo_vmdb
   :members: cleanup, initdb, normalize, export_table, CSVImporter
